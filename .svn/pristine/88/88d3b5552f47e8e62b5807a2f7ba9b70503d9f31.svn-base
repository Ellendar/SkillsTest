package com.englandlogistics.scm.skills_test.third;

import java.util.List;

public class SumOfThree {
	
	/**
	 * Given a list of integers, find the index of 3 such integers that sum to zero. 
	 * For example: given the list [3, 15, -12, 10, -2, -3], [1, 2, 5] would be a solution.  (15 + -12 + -3) 
	 * 
	 * Bonus: What is the algorithmic complexity of your solution? Can this be solved in O(n^2 log(n))?
	 * 
	 * @param args A list of integers.
	 * @return The Triple describing any such solution, or null if none exists.
	 */
	public static Triple sumOfThree(List<Integer> args) {
		throw new RuntimeException();
	}
	
	
}
