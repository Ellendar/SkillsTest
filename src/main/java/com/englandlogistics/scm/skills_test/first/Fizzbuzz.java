package com.englandlogistics.scm.skills_test.first;

import java.util.List;


public class Fizzbuzz {
	
	/**
	 * Create a list of n strings using the following procedure.
	 * For each integer from 1 to n:
	 * if the number is divisible by 3 and 5, add "fizzbuzz"
	 * else if the number is divisible by 3, add "fizz"
	 * else if the number is divisible by 5, add "buzz"
	 * else add the number
	 * For example: the first 5 such members should be ["1', "2", "fizz", "4", "buzz"]
	 * 
	 * @param n 
	 * @return A list as described.
	 */
	public static List<String> fizzBuzz(int n) {
		throw new RuntimeException();
	}
}
